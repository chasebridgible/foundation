interface:
  display_name: "Backfill Process / Action Map"
  short_description: "Map concrete processes and actions"
  default_prompt: "Use $backfill-map-actions to derive the Process / Action Map from this repo evidence before drafting job specs."
